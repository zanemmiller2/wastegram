import 'package:flutter/material.dart';

class RedCircularProgressIndicator extends StatelessWidget {
  @override
  Widget build(BuildContext context) => const CircularProgressIndicator(color: Colors.red);
}